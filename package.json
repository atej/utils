{
  "type": "module",
  "scripts": {
    "prepare": "is-ci || husky",
    "format": "prettier . --ignore-unknown --write --cache --check",
    "typecheck": "tsc",
    "test": "vitest",
    "check": "npm run format && npm run typecheck && vitest run",
    "precommit": "lint-staged"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250807.0",
    "@total-typescript/ts-reset": "^0.6.1",
    "@types/node": "^24.2.0",
    "@vitest/coverage-v8": "^3.2.4",
    "@vitest/ui": "^3.2.4",
    "husky": "^9.1.7",
    "is-ci": "^4.1.0",
    "lint-staged": "^16.1.4",
    "prettier": "^3.6.2",
    "prettier-plugin-organize-imports": "^4.2.0",
    "type-fest": "^4.41.0",
    "vitest": "^3.2.4"
  }
}
